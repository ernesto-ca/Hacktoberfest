name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Welcome to Wir Coders! , Make contributions to earn your Hacktober Swag!'' first issue'
        pr-message: 'Delighted to have you on board, contribute to your'' first pr'
